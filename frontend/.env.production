# Production environment variables for Next.js build
# These are public values that get embedded in the client-side JavaScript bundle

# Use the same domain as frontend (ai.mavik-ssot.com) for SSL certificate match
NEXT_PUBLIC_BACKEND_URL=https://ai.mavik-ssot.com/api
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-2_fuVW3us9r
NEXT_PUBLIC_COGNITO_CLIENT_ID=5891okbc5015jt5po252mufcbo
