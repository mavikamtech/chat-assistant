"""Mavik AI root package for Poetry installation.

This package holds no runtime code; it exists so the monorepo root is
installable for tooling (ruff/mypy/pytest configs) in production-like setups.
"""
